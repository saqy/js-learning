let numbers = [1, 2, 3];

console.log(Array.isArray(numbers));

let name = { name: "Sadaqat" };

// console.log(Array.isArray(name));

if (Array.isArray(name)) {
    console.log("It is not array");
} else {
    console.log("It is array");
}