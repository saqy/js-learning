//ternary operators
console.log(id === 100 ? 'CORRECT' : 'INCORRECT');

// strict equal(===) 3 === var1
// strict not equal(!==) var1 !== "3" 3 !== '3'

//destructuring
//this
//Let’s start with a simple function. When you define a variable in the global scope, it belongs to window object. 
//Now, you know, this is just an object. So, in the global scope, this refers to window. But the value of this varies depending on where you called it. 
//Inside a function, this refers to the object that function belongs to.

//es5 vs es6
//in es6 symbol, let and const, arrow function no function keyword, for of loop,
// map vs object
//promises